TO DO'S
# calculate option greeks - DONE ✅
# filter out based on user preference. (ex - user wants to only see strikes with 1000+ volume)
# filters continued... min volume, min open interest, max bid-ask spread, only calls / only puts,
only ITM / OTM / ATM, strike range around ATM, min/max IV, expiration filters

# point out where the ITM(ATM) strikes are for both calls and puts
# Spread calculation, Show the bid-ask spread: (ask-bid)
# Mid-price calculation: ((bid+ask) / 2)
# Gamma Exposure (GEX) (shows where market makers are hedging.)
# Track premium vs stock price - the original idea.
# Premium strength indicator - expected premiums(model) vs actual premium (Option overpriced/underpriced)
# detect unusual volume (FLAG: Unusual options activity)
# option payoff (is it worth staying in the position anymore)
# more ideas on chatgpt ("thetachart" chat) like AI features...
# tools similar to tools on unusual whales.
# trading view style charts and indicators
# add stock info (open, prev close, dividend, 52 week high/low, total assets, etc..)
# compare regular avg premiums to current premiums (ex. 1dte itm is 3.50, but today is 4.50.
                                                            Give "premiums is more expensive than usual alert")

# --- JOURNAL --- #
Suggested flow for the journaling feature

journal/ backend flow should look like this:

user uploads CSV
csv_import_service.py reads it with pandas
broker_column_mapper.py maps columns to ThetaChart standard names
trade_parser_service.py turns rows into normalized trade objects
trade_cleaning_service.py fixes formatting/errors
pnl_service.py calculates metrics
calendar_service.py groups pnl by day
UI displays:
trade table
stats cards
PnL calendar
best/worst trades